---
name: generate-spec
description: Generates an ASDD technical spec in .github/specs/.
---

# Spec Generator

Translates raw `.github/requirements/` documents into `.github/specs/` markdown structures. Ensure it starts as `DRAFT`.
